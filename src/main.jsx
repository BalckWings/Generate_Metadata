import React from 'react'
import ReactDOM from 'react-dom/client'
import MetadataGenerator from './MetadataGenerator.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <MetadataGenerator />
  </React.StrictMode>,
)